spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/clientedb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true

services:
  pedido:
    url: http://pedido-service:8082